# ==========================================
# Radio Super A1 - Archivos a Ignorar por Git
# Este archivo mantiene nuestro repositorio limpio y seguro.
# ==========================================

# ==========================================
# Archivos del Sistema Operativo
# ==========================================
# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux
*~

# ==========================================
# Editores de Código y Entornos de Desarrollo
# ==========================================
# VS Code
.vscode/
!.vscode/extensions.json
!.vscode/settings.json

# Otros editores (Vim, Sublime, etc.)
*.swp
*.swo
*~

# ==========================================
# Dependencias y Build (Para futuras expansiones)
# ==========================================
# Si usas Node.js (npm/yarn) para herramientas de construcción
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Carpetas de salida generadas por herramientas como Vite, Webpack, etc.
dist/
build/
.cache/

# ==========================================
# Logs y Errores del Servidor o Aplicación
# ==========================================
*.log
logs/

# ==========================================
# ARCHIVOS SENSIBLES (¡MUY IMPORTANTE!)
# ==========================================
# Nunca subas archivos con claves, tokens o contraseñas.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ==========================================
# Recursos Multimedia y Assets Pesados
# ==========================================
# Ignora archivos de audio/video originales sin comprimir.
# Sube solo las versiones optimizadas para la web.
*.wav
*.aiff
*.mov
*.avi
# Nota: Si tienes archivos .mp3 o .mp4 optimizados, sí puedes incluirlos.

# Ignora archivos de diseño originales (Photoshop, Illustrator, etc.).
# Solo sube las imágenes exportadas para la web (.jpg, .png, .webp).
*.psd
*.ai
*.sketch

# ==========================================
# Copias de Seguridad y Temporales
# ==========================================
*.bak
*.tmp
*.temp